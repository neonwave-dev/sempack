# Which planning/tracking surfaces this repo maps to.
# Read by the `triage` skill (and any skill that needs the repo↔project link).
# Fill in the surfaces this repo actually uses; delete the ones it doesn't.

# linear:
#   team: TEAMKEY          # Linear team key (home machines, ISSUE_TRACKER=linear)
#   project: sempack       # Linear project name or id

# jira:
#   project: PROJKEY       # Jira project key (work machines, ISSUE_TRACKER=jira)

obsidian:
  project: SemPack         # folder name under PLANNING_VAULT_ROOT (01 - Projects/Software)

github:
  repo: neonwave-dev/sempack
